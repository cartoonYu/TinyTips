<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cartoon.tinytips">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.cartoon.tinytips.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_path" />
        </provider>

        <activity android:name=".Main.Main"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".HomePage.AddNote.AddNote"
            android:launchMode="singleTask"/>
        <activity android:name=".HomePage.Details.HomePageDetails"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".HomePage.Details.Revamp.HomePageDetailsRevamp"
            android:launchMode="singleTask"/>

        <activity android:name=".Personal.Profile.PersonalProfile"
            android:launchMode="singleTask"/>
        <activity android:name=".Personal.Profile.RevampSchool.PersonalProfileSchool"
            android:launchMode="singleTask"/>
        <activity android:name=".Personal.Profile.RevampResume.PersonalProfileResume"
            android:launchMode="singleTask"/>
        <activity android:name=".Personal.Profile.RevampSignature.PersonalProfileSignature"
            android:launchMode="singleTask"/>

        <activity android:name=".Personal.Security.PersonalSecurity"
            android:launchMode="singleTask"/>
    </application>
</manifest>
